#!/bin/sh

DST=/usr/include/"$(basename $PWD)"
echo "Targeting destination: \"$DST\""

# Create directory.
[ ! -d "$DST" ] && mkdir -pv "$DST"

# Install headers.
cp -vr inc/* "$DST" || ( printf "\nPre-installation failed.\n" >&2 && exit 1 )

printf "\nProcedure succeeded.\n"
